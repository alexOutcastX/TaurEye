# Shell scripts must keep LF endings — they run on the Linux VM (and CRLF breaks
# the shebang / bash). Force LF regardless of the committer's core.autocrlf.
*.sh        text eol=lf
*.conf      text eol=lf
Caddyfile   text eol=lf
*.service   text eol=lf
